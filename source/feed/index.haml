---
pageable: true
per_page: 10
---

- content_for(:title, 'Feed')
- content_for(:description, 'We’re a digital design &amp; development shop focused on user experience. We’re a small team of designers, thinkers, strategizers, do-ers &amp; storytellers packing a lot of experience across disciplines. Staying small keeps our hands dirty, our overhead lean, our turnarounds fast, and our clients happy.')
- content_for(:foreground, '#fff')
- content_for(:background, '#222')

- if recent_pins
  %article.feed
    %hr
    .post-container
      .post-type
        %i.icon.icon-pintrest
      .post
        - recent_pins['body'].first(8).each do | pin |
          %span{:class => 'active feed-img', :data => {:href => 'http://pinterest.com/oneiota/pins/', :img => pin['src'], :alt => pin['desc']}}
          /%img{:src => pin['src'], :alt => pin['desc'], :title => pin['desc']}
        %p
          %a{:href => 'http://pinterest.com/oneiota/pins/',:target=>'_blank',:title => '@oneiota on Pintrest'} Latests pins @oneiota

- if behances
  - behances.each do | behance |
    %article.feed
      %hr
      .post-container
        .post-type
          %i.icon.icon-behance
        .post
          %img{:src => behance['covers']['404']}
          /%span{:class => 'active feed-img', :data => {:href => dribbble['url'], :img => dribbble['image_url'], :alt => dribbble['player']['username']}}
          %p
            %a{:href => behance['url'],:target=>'_blank',:title => behance['name']} New project #{behance['name']} added to our behance
        .post-date
          %span{:class => 'published', :data => {:published => clean_date(behance['created_on'])}}

- if dribbbles
  - dribbbles.each do | dribbble |
    %article.feed
      %hr
      .post-container
        .post-type
          %i.icon.icon-dribbble
        .post
          %span{:class => 'active feed-img', :data => {:href => dribbble['url'], :img => dribbble['image_url'], :alt => dribbble['player']['username']}}
          %p
            %a{:href => dribbble['short_url'],:target=>'_blank',:title => dribbble['player']['username']} Posted #{dribbble['title']} to our dribbble
        .post-date
          %span{:class => 'published', :data => {:published => dribbble['created_at']}}

-if tweets
  - tweets.each do | tweet |
    %article.feed
      %hr
      .post-container
        .post-type
          %i.icon.icon-twitter
        .post
          %p=clean_tweet(tweet.text)
        .post-date
          %span{:class => 'published', :data => {:published => tweet.created_at}}

- blog.articles.each_with_index do |article, i|
  - if article.data.type == 'post'
    %article.feed
      %hr
      .post-container
        .post-type
          %i.icon.icon-post
        .post
          %h2= link_to article.title, article
          - if article.data.images
            .post-image-containter
              .post-images
                - article.data.images.each_with_index do |img, index|
                  - imgsrc = 'feed/' + img.keys.first
                  - imgtitle = img.values.first
                  - if index == 0
                    %span{:class => 'active feed-img', :href => '#',:data => {:img => imgsrc, :alt => imgtitle}}
                  - else
                    %span{:class => 'feed-img', :href => '#',:data => {:img => imgsrc, :alt => imgtitle}}
          = article.body
        .post-date
          %span{:class => 'published', :data => {:published => article.date}}

- if paginate
  .paginate
    %p Page #{page_number} of #{num_pages}
    - if next_page
      %p= link_to 'Next page', next_page
    - if prev_page
      %p= link_to 'Previous page', prev_page
